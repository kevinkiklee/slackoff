json.partial! "api/channels/channel", channel: @channel,
                                      messages: @messages,
                                      user_count: @user_count,
                                      users: @users
